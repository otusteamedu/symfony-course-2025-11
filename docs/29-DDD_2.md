DDD
===
## Теория (60 мин)
### RAD
- Пример контроллера - какие есть недостатки?

### Table-Driven Development
- Active Record
- ORM - отвязываем от таблицы
- Анемичная модель

### Model Driven Development
- Фокус не на структуре таблиц, а на кодовой модели объектов предметной области, их структуре, правила и поведении

### Блоки: Entity
- Объект, который имеет уникальный идентификатор и жизненный цикл.
- Проверяет свое состояние и обеспечивает целостность данных.
- При создании проверяет инварианты.
- Каждая мутация переводит из одного целостного состояния в другое целостное.
- Проверки и в конструкторе и в мутациях = дублирование проверок  - можно вынести в отдельный объект.

### Блоки: Value Object
- Объект-значение, который не имеет уникального идентификатора и не изменяется.
- Проверяет свое состояние, придавая смысл значениям.
- Два разных объекта с одинаковым значениями считаются равными.

### Вопрос: где Entity и где Value Object на схеме? про куплюру?
- Сущности и объекты значения помогают нам придать смысл примитивным типам и структурам PHP.

### Блоки: Aggregate
- Несколько связанных сущностей, которые меняются вместе в рамках одной транзакции
- Всегда есть главная сущность
- Может состоять из одной сущности

- Агрегат не может иметь объектных ссылок на другие агрегаты, вместо этого - идентификаторы.

- Агрегат - это про тактические границы. Вы сами определяете где они проходят, нет правил. Чем меньше тем лучше
    - Пример с MealPlan -> Days -> Meals -> Recipes -> Ingredients -> Measurement Units
    - Тренировка: выделите на схеме агрегаты

- Если несколько агрегатов меняются одновременно, то это может привести к рассинхрону... Придется жить с Eventual Consistency. Не везде применимо, например в банковских транзакциях, но в 90% случаях да.


### Repository
- Нужно упомянуть, что репозиторий мы делаем только для агрегатов!
- Для составных частей не делаем
- Репозиторий реализует интерфейс Collection
- Репозиторий не содержит бизнес логики, не создает объекты, только сохраняет и извлекает агрегаты.

### Event Driven Development
- Агрегаты генерируют события
- События отправляются в шину событий
- Доменные события обрабатываются слушателями в рамках той же транзакции.
- Доменные события могут быть оптравлены в шину сообщений и слушаться другими контекстами

- На самом деле все сложнее, доменные события обрабатываются в рамках той же транзакции внутри домена, а для кросс-контекстной выделяют интеграционные события.

### Event Sourcing
- События хранятся в журнале событий и объекты восстанавливаются из них.

### CQRS
- Команды меняют состояние и ничего не возвращают
- Запросы возвращают результат и ничего не меняют

- Зачем нужен?
    - Разделение ответственности между командами и запросами
    - Оптимизация производительности запросов, в командах - DQL, в запросах чистый SQL
    - Чтения можно масштабировать независимо (например, на реплики).
    - Команды можно оптимизировать за счёт очередей, асинхронных команд и event sourcing.

## Практика
Добавим новый модуль - Sales, твиттер по подписке.
- Реализуем подписочную модель SaaS.
- Реализовано только ядро (`Application Core`)
- В рамках примера мы не реализуем реализацию Doctrine - суть Model-Driven Development в том, что начинаем разработку с ядра, инфраструктурой занимаемся потом.
- Вместо реальных инфраструктурных зависимостей в контейнере `config/services.yaml` мы подключили тестовые `InMemory`-реализации репозиториев, что позволяет и контроллеры сразу реализовать.

### Язык
Показываем вначале маппинг терминов на объекты,
потом действия с этими терминами,
потом события на каждое из событий.
+ добавляем внешний сервис `PaymentGatewayInterface` для получения ссылки на оплату

- `Customer` - клиент (соответствует User из основного монолита)

- `Product` - продукт или тарифный план, на который подписываемся

- `Subscription` - подписка.
  Действия с ней и возникающие события:
    - `Create`   -> `SubscriptionCreated`
    - `Activate` -> `SubscriptionActivated`
    - // Берем только первые два
    - `Cancel`   -> `SubscriptionCanceled`
    - `Renew`    -> `SubscriptionRenewed`


- `Invoice` - счет на оплату. Подписка каждый следующий период генерирует счет на оплату
  Действия с ним и возникающие события:
    - `Create` -> `InvoiceCreated`
    - `Pay`    -> `InvoicePaid`
    - `Expire` -> `InvoiceExpired`

- `PaymentGateway` - платежный шлюз

### Создаем новые модули
- Sales
  ```shell
  mkdir sales
  mkdir sales/tests
  mkdir sales/src
  mkdir sales/src/Domain
  mkdir sales/src/Domain/Model
  mkdir sales/src/Application
  mkdir sales/src/Infrastructure
  ```

- Shared Kernel
```shell
mkdir sharedKernel
mkdir sharedKernel/tests
mkdir sharedKernel/src
mkdir sharedKernel/src/Domain
```

- Добавляем неймспейсы в composer.json:
```json
{
  "autoload": {
    "psr-4": {
      "bravik\\Shared\\": "sharedKernel/src/",
      "bravik\\Sales\\": "sales/src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "bravik\\Shared\\Tests\\": "sharedKernel/tests/",
      "bravik\\Sales\\Tests\\": "sales/tests/"
    }
  }
}
```

- Настраиваем юнит-тесты
```shell
git checkout sales/phpunit.xml
git checkout sales/bootstrap.php
git checkout sharedKernel/phpunit.xml
git checkout sharedKernel/bootstrap.php
git checkout Makefile

make up
make test
```

### Работа с моделями
#### Создаем модели
```shell
touch sales/src/Domain/Model/Customer.php
touch sales/src/Domain/Model/Product.php
touch sales/src/Domain/Model/Subscription.php
touch sales/src/Domain/Model/Invoice.php

### И тесты к ним
touch sales/tests/Domain/Model/CustomerTest.php
touch sales/tests/Domain/Model/ProductTest.php
touch sales/tests/Domain/Model/SubscriptionTest.php
touch sales/tests/Domain/Model/InvoiceTest.php
```

- Набросок `Customer`:
```php
<?php

declare(strict_types=1);

namespace bravik\Sales\Domain\Model;

class Customer
{
    public string $id;
    public string $nameFirst;
    public string $nameLast;
    public string $email;

    public function __construct(
        string $nameFirst,
        string $nameLast,
        string $email
    ) {
        $this->id = $id;
        $this->nameFirst = $nameFirst;
        $this->nameLast = $nameLast;
        $this->email = $email;
    }
}
```

Добавляем и показываем идентификатор OId (чекаут из ветки с уроком + тест)
```php
<?php

declare(strict_types=1);

namespace bravik\Sales\Domain\Model;

use bravik\Shared\Domain\Model\OId;

class Customer
{
    public OId $id;
    public string $nameFirst;
    public string $nameLast;
    public string $email;

    public function __construct(
        OId $id,
        string $nameFirst,
        string $nameLast,
        string $email
    ) {
        $this->id = $id;
        $this->nameFirst = $nameFirst;
        $this->nameLast = $nameLast;
        $this->email = $email;
    }
}
```

- Оборачиваем `nameFirst` и `nameLast` в `ValueObject` `Name` (чекаут из ветки с уроком + тест):
- Оборачиваем `email` в `ValueObject` `Email` (чекаут из ветки с уроком + тест):
- Добавляем геттеры и фабричный метод (понадобится когда события добавятся)

```php
<?php

declare(strict_types=1);

namespace bravik\Sales\Domain\Model;

use bravik\Shared\Domain\Events\EventsTrait;
use bravik\Shared\Domain\Model\AggregateRootInterface;
use bravik\Shared\Domain\Model\Email;
use bravik\Shared\Domain\Model\Name;
use bravik\Shared\Domain\Model\OId;

class Customer
{
    public OId $id;
    public Name $name;
    public Email $email;

    public function __construct(
        OId $id,
        Name $name,
        Email $email
    ) {
        $this->id = $id;
        $this->name = $name;
        $this->email = $email;
    }

    public static function create(Name $name, Email $email): self
    {
        return new self(OId::next(), $name, $email);
    }

    public function getId(): OId
    {
        return $this->id;
    }

    public function getName(): Name
    {
        return $this->name;
    }

    public function getEmail(): Email
    {
        return $this->email;
    }
}
```


- Достаем остальные не очень важные модели
```shell
git checkout sales/src/Domain/Model/Product.php
git checkout sales/src/Domain/Model/Price.php
```

- Добавляем модель `Subscription`
- Обращаем ввнимание, что на пользователь и продукт ссылаемся по идентификатору, а не объекту.
- В фабричном методе обращаем внимание на логику: устанавливается в статус PENDING
- Конструктор получает все поля

```php
<?php

declare(strict_types=1);

namespace bravik\Sales\Domain\Model\Subscription;

use bravik\Sales\Domain\Model\Price;
use bravik\Shared\Domain\Model\OId;
use DateTimeImmutable;


class Subscription
{
    public function __construct(
        private OId $id,
        private OId $customerId,
        private OId $productId,
        private Price $price,
        private Status $status,
        private DateTimeImmutable $startDate,
        private ?DateTimeImmutable $endDate = null
    ) {
    }

    public static function create(
        OId $customerId,
        OId $productId,
        Price $price,
        DateTimeImmutable $startDate,
        ?DateTimeImmutable $endDate = null
    ): self {
        $subscription =  new self(
            OId::next(),
            $customerId,
            $productId,
            $price,
            Status::PENDING,
            $startDate,
            $endDate,
        );

        return $subscription;
    }

    public function getId(): OId
    {
        return $this->id;
    }

    public function getCustomerId(): OId
    {
        return $this->customerId;
    }

    public function getProductId(): OId
    {
        return $this->productId;
    }

    public function getPrice(): Price
    {
        return $this->price;
    }

    public function getStartDate(): DateTimeImmutable
    {
        return $this->startDate;
    }

    public function getEndDate(): ?DateTimeImmutable
    {
        return $this->endDate;
    }

    public function isPending(): bool
    {
        return $this->status === Status::PENDING;
    }

    public function isActive(): bool
    {
        return $this->status === Status::ACTIVE;
    }

    public function isCancelled(): bool
    {
        return $this->status === Status::CANCELLED;
    }

    public function isExpired(): bool
    {
        return $this->status === Status::EXPIRED;
    }
}
```

#### Создаем события в агрегате
```shell
git checkout sales/src/Domain/Events/SubscriptionCreated.php
git checkout sharedKernel/src/Domain/Events/EventInterface.php
git checkout sharedKernel/src/Domain/Events/AbstractEvent.php
git checkout sharedKernel/src/Domain/Events/EventsTrait.php
git checkout sharedKernel/src/Domain/AggregateRootInterface.php
```

- Фабричный метод создает событие
- Добавим событие `SubscriptionCreated` (чекаут из ветки с уроком + тест)
- Показываем что в `AbstractEvent`
- Тонкие vs толстые события

- События создают агрегаты, но агрегаты не отправляют событие, нужно записать где-то - `EventsTrait` и `AggregateRootInterface` + показыаваем как записываются в `create()`

#### Мутаторы агрегата
- Чекаут из ветки
- На каждый глагол из нашей схемы создаем метод-мутатор
- Показываем мутаторы `activate()`, `cancel()`, `expire()`, `renew()`
- Каждый из них создает событие, которое записывается в `EventsTrait`
- Они так же кидают кастомные исключения `DomainException`
- Место для бизнес логики.

#### Составной агрегат Invoice
- чекаут из ветки
- Показываем пример составного агрегата
- Акцентируемся на методах, через которые мы собственно работаем с LineItem - добавляем его
- Смотрим на метод `pay()`: туда и парааметры передаются, и три поля меняются и событие нужное нам для активации подписки создается

#### Прогоним тесты еще разок для модели

### Сценарии использования / Сервисы
```shell
git checkout sales/src/Application/UseCases
```
- В командах должны быть примитивы, но я использую `OId` как исключение
- Команда имеет порты для инфраструктурных сервисов - интерфейсы. Показываем что за сервисы есть и реализации-моки (так как реальных пока нет)
- Показываем, что все это уже покрыто тестами
- Показываем моки InMemory репозитории для тесты
- Обращаем еще раз внимание, что у нас вообще нет инфраструктуры, мы занимаемся чистой бизнес-логикой
- Показываем сценарий про подписку
- Остальные сценарии особо не объясняем

#### Подписка (Subscriptions/Create)
- Пользователь подписывается на продукт
- Создается подписка в статусе `pending`
- Пользователю возвращается идентификатор новой подписки
- По событию `SubsriptionCreated` создается счет на оплату
- Тут `$endDate` по хорошему должен быть где-то внутри домена, а не в команде, так как это бизнес-логика
- Эта команда нарушает принцип того, что команда не возвращает результат

#### Получение ссылки на оплату
- Пользовательский UI по идентификатору подписки поллит бэкэнд для получения ссылки на оплату
- Когда инвойс будет создан, бэкэнд вернет ссылку на оплату

#### Оплата и активация подписки
- Пользователь переходит по ссылке и оплачивает счет
- Платежный счет вызывает HTTP вебхук при успешной оплате. Действие `Invoice.Pay`
- По событию `InvoicePaid` подписка переводится в статус `active`. Действие `Subscription.Activate`


### Добавляем фасад
- В монолите можно и напрямую в целом использовать наши сцеенарии - вы сами выбираете уровень абстрации
- Но если все же мы хотим изоляцию, то можно добавить API в слое `Presentation`
- Если мы будем код использовать напрямую, пойдет общий интерфейс для модуля или фасад

```shell
git checkout sales/src/Presentation
```
- Интерфейс `SalesApiInterface` - это фасад, который объединяет все сценарии использования
- Фасад использует на входе и на выходе только примитивы, либо DTO объекты со слоя `Presentation`. Все эти DTO - чсть контракта
- Реализацию храним тут же.
- Реализация фасада перенаправляет вызов каждого метода нужному сценарию использования
    - Обращаем внимание на `ServiceSubscriber` и ленивую загрузку сервисов
    - Обращаем внимание, что мы кидаем `Throwable` на все исключения. В контракте нужны свои исключения и в фасаде надо переводить с языка Application в язык контракта.
    - Это может казаться доп прослойкой и лишним кодом, но когда мы сделаем другую реализацию фасада, например HTTP, для микросервиса, то это будет очень удобно.
- В случае вынесения в микросервис, нам останется только реализовать интерфейс, который будет отправлять HTTP запросы вместо прямого доступа.
- Можно не делать все это, а просто использовать сценарии использования напрямую, это уже улучшит организацию кода


### Используем в контроллерах
```shell
git checkout src/Controller/Web/Sales/GeneratePaymentLink
git checkout src/Controller/Web/Sales/Subscribe
```
- Показываем на пример `Subscribe`
- Контроллеры здесь - часть легаси API монолитного приложения.
- Мы не делаем контроллеры в sales, а продолжаем пилить монолит.
- Но при этом эти контроллеры должны соблюдать границу и обращаться в модуль только через наш фасад и контракт.
- Альтернативно, можно использовать сценарии использования напрямую

- В `Subscribe` мы создаем подписку, получаем идентификатор и фронтенд будет DDOSить этот контроллер, чтобы получить ссылку на оплату.
- Можно еще обратить внимание на то, что идентификатор тут по хорошему должен создаваться не в обработчике команды, а в реализации фасада `Sales`. Так он дернет одну команду и отдаст сразу же идентификатор, а команда не будет ничего возвращать.

- Во-вторых, есть паттерн `ApiComposition`


### Подключаем моки в качестве реализаций репозиториев в контейнере
- Делаем конфиг с сервисами для модуля Sales
```shell
git checkout config/services.yaml
```

- Делаем модуль бандлом для удобного подключения конфига
```shell
git checkout sales/src/SalesBundle.php
```

- Подключаем бандл в `bundles.php`

- Тест создаания подписки (`client.http`)
```http request

POST {{host}}/api/v1/create-paid-subscription
Content-Type: application/json

{
  "userId": "7bc55b76-3c6b-4644-9251-9c093b2c5a16",
  "productId": "8bc55b76-3c6b-4644-9251-9c093b2c5a17"
}
```

Подводим итог: мы полностью реализовали всю бизнес логику и даже прокинули её в контроллеры, при этом ни одной реально инфраструктурной зависимости не было. Теперь можно начать реализовывать инфраструктуру

Инфраструктуру мы опускаем.

### Extra: Пользователь в разных контекстах
- К примеру, `Customer` из `Sales` это не одно и то же, что `User`
- Он может  жить в отдельной таблицы и в отдельной БД
- Необходимо использовать события для того чтобы синхронизировать их
- Альтернативно: маппинг на одну таблицу
- Альтернативно: прячем в репозиторри детали того, как мы вытаскиваем пользователя, хоть через легаси репозиторий основной системы.
- Полезно при рефакторинге: Можно быстро получить готовый продукт, и постепенно отсоединять его от монолита

### Extra: Custom Doctrine Types

### Extra: Контроль архитектуры
Для контроля архитектуры настроен Deptrac.  
См. `sales/deptrac-layers.yaml`  
Запуск: `make deptrac` (см. `Makefile` в корне проекта)